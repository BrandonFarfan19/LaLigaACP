## Project

**La Liga ACP** — a sports tournament web app (fixtures, standings, teams, players, match results).
In addition a betting platform.

### Mobile first

Design and build every screen for phone widths first (~390px), then scale up with min-width media queries. Touch targets at minimum 44px. Never ship a layout that only works on desktop and gets squeezed down.

### Visual style: pixel art, 90s video game

**The entire UI is pixel art — fonts, chrome, artwork, motion.** Nothing on screen may look like a modern web app. This rule overrides every other aesthetic preference; if a technique is not achievable in a 16-bit sprite, it does not go in.

**Typography**

- Pixel/bitmap faces only, self-hosted through `@fontsource` — never a CDN link. `Press Start 2P` for display (headings, names, scores, nav), `Silkscreen` for body and labels. Both are set as `--font-display` / `--font-body`.
- Font sizes come from the `--text-*` tokens and are **whole multiples of 8px**. A bitmap glyph at 13.5px lands between physical pixels and turns to mush.
- **Never synthesise weight or style.** Press Start 2P ships one weight: use `font-weight: 400`. No `700`/`800`, no italic, no `letter-spacing` in `em`.
- `-webkit-font-smoothing: none` stays on `body`. Antialiasing is what makes a pixel font look like a blurry web font instead of a sprite.

**Chrome and shape**

- **No `border-radius`. No blurred shadows. No `backdrop-filter`. No smooth gradients.** These are the four tells of a modern UI.
- Borders, bevels and shadows are built from hard offsets in multiples of `--px` (one "fat pixel"). Use the `.pixel-box`, `.pixel-bevel` and `.pixel-shadow` primitives in `global.css`.
- Gradients must be **banded**: hard color stops that step, imitating dithering. A continuous gradient is not allowed.
- Spacing sits on a strict 8px grid via the `--space-*` tokens.

**Color**

- A small, fixed palette, like a console with limited colors. Adding a color is a deliberate decision — extend the tokens in `global.css`, never hardcode a hex in a component.

**Artwork**

- Logos and crests are rendered small through `<PixelImage />` (32–96px) and upscaled by CSS with `image-rendering: pixelated` (the `.pixelated` class). This turns smooth artwork into real pixel art instead of a soft enlargement. Never render them at full size.

**Motion**

- Transitions and animations use `steps()`, never `ease`/`linear`. Movement snaps between frames the way sprite animation does.
- Every animation must be disabled under `prefers-reduced-motion: reduce`.

### Data: the league reads the API (T-22)

**Nothing on screen is hardcoded any more.** The league pages (landing, `/posiciones`, `/plantilla/:id`) read the public API (`/public/...`, no session) through `src/lib/league.ts`, the one place that knows the API's Spanish names and maps them to the types of `src/types/index.ts`. `src/data/` holds only the sample ratings of the radar (below). Rules:

- **One data-access layer.** All reads go through `src/lib/` (`listSports`, `listCompetitions`, `listTeams`, `listFixture`, `listStandings`, `getTeamWithSquad`, plus `readLeagueChoice` in `league-view.ts`). Pages and components call those functions and never build API paths themselves.
- **Pages read in their route `loader`**, which calls `src/lib/` and hands the result to the page with `useLoaderData()`. Components receive data as props; they never fetch on their own. A route renders complete, so anchors like `/#fixture` exist when scrolling runs.
- **Relations arrive resolved**, the way the API sends them (a match carries both teams and its competition). Ids are the API's numeric ids as text, and the routes use them: `/plantilla/42`. **Every row is mapped**, the table included (`standingOf` → `ResolvedStanding`): no Spanish key from the API ever reaches a screen.
- **400 and 404 are both "no existe"** (`isMissing`): `/plantilla/:id` with an id that isn't one, or that no team has, renders the not-found page. Anything else (no network, 5xx, 429) keeps the page with its notice and "Reintentar" (`LeagueNotice`), never a blank screen.
- **A body that isn't the contract** (a `null`, a text, an object without the field a screen reads, a match state nobody knows) is an `ApiError` with status 0 and code `BAD_RESPONSE` (`badResponse` in `league.ts`), so it lands on that same notice with "Reintentar" instead of throwing a `TypeError` onto the error page.
- **Which competition is on screen (D-021, BR-048):** `?deporteId=` and `?competicionId=` in the page URL, chosen with visible options (`CompetitionPicker`, D-017), so the view can be shared. Without them the rule lives in one place, `defaultCompetition`: the competition of the **next scheduled match**; with nothing scheduled, that of the **most recent match really played** (`en_curso` or `finalizado`), skipping cancelled ones (they never have a score, BR-049, nor count in the table, BR-050); if every match was cancelled, the most recent of those; and with no matches at all, the first competition of the list. One page in proximity order decides it, and only a page that is nothing but cancelled matches asks for the scheduled one exactly. **With a sport chosen, every read of that rule carries `deporteId`**, so the competition comes from that sport's own matches instead of falling back to its first competition by name, empty or not (T-22 second fix).
- **The fixture is asked for by competition**, 100 at a time (`?competicionId=`, `?jornada=`), never by walking the whole table. It reads at most 5 pages: beyond that `listFixture` reports `truncated` and the section says the list was cut, instead of dropping matches in silence. The landing reuses the matches that chose the competition when that single read held **every** match there is, so it asks once, not twice.
- **The table shows every column of BR-050 (D-023):** played, won, drawn, lost, goals for and against, difference and points. Ten columns never fit a phone, so `/posiciones` puts the table in a container with **its own horizontal scroll**, focusable and labelled (`role="region"`, `tabindex="0"`), and the page itself never scrolls sideways; the team cell is pinned to the left edge so a scrolled row stays identifiable. Headings are the usual abbreviations (PJ, PG, GF…) with their full words beside them for screen readers.
- **Crests and photos come from the API** as an `https://` URL or a path inside the site, so they are not build-time renditions: they are shown with `<img>` and `pixelated` at a fixed size (`Crest`), never inline SVG. `PixelImage` and `?pixel=` presets stay for the artwork that does live in `src/assets/` (logo, pitch, portrait, backdrops).
- Keep the output a static SPA (`vite build` to `dist/`): the API answers on the same origin under `/api` (D-006).

**The radar ratings are sample data (D-022).** Everything else on the league screens is real since T-22. The six attributes (`PlayerStats`: shooting, passing, strength, defense, speed, dribbling) are not: no business rule defines them and the schema doesn't store them. `src/data/player-stats.ts` generates them from the player's **real id** (`statsForPlayer`), each a whole number from 70 to 90, so they stay the same on every reload and device. The player's card says so out loud, and the pitch says its layout is a sample too (the shirt number is the real one). They are shown as a pixel-art radar (`PlayerStatsDialog.tsx`, drawn by `src/utils/pixel-radar.ts`) plus a table, opened from `/plantilla/:id`.

### Database design

This section summarizes the target schema. Full detail (every column, constraint and the ER diagram) lives in [EsquemaBD.md](EsquemaBD.md), in Spanish. The schema now models the betting pool from `docs/business-rules.md` (see **Betting platform** below) — `disciplina`/`mercado`/`apuesta` are gone, replaced by `deporte`/`competicion` and `ticket`/`seleccion`.

- **A local MySQL 8.4 runs in Docker** (`compose.yaml`, credentials in `.env`, see `.env.example`). `db/init/01-schema.sql` creates every table and `db/init/02-catalogos.sql` loads only the catalog rows. Setup, connection and reset steps are in the README.
- **The league pages read it through the public API since T-22** (`/public/...`, see **Data** above); the site is still a static SPA served next to the API. Real league rows are loaded by an admin through the panel (T-21), never by hand: the one command that writes data by itself is the development-only sample data command (D-013, D-016): `npm run server:seed:dev -- --yes-dev-data` loads fictitious data and records every row it creates in `dato_demo` (a development-only table the command creates, not part of the app schema); `npm run server:seed:dev:clean -- --yes-dev-data` removes only the recorded rows (plus what the sample accounts did) and refuses, deleting nothing, when real data hangs from them. It refuses to run unless `NODE_ENV=development` is set in the real environment (not the default), the database is exactly `DEV_SEED_DATABASE` (never a test one) and the flag is given. Nothing loads by itself.
- **The backend (`server/`) is an Express app, and must stay one.** No other framework (Fastify, Nest, Koa, Next API routes…). It already connects to this database through `mysql2` (see **Backend stack** below). As of T-04 it has `GET /health`, accounts (register, login, sessions, roles) and participant validation under `/admin/participantes`, plus the catalog, matches, the public API, selection checks (T-09), ticket confirmation (T-10), the bettor's history (T-11), result registration (T-12), goals and media (T-13), bet settlement with points (T-14), the pool ranking with the admin's pool statistics (T-15) match cancellation with refunds (T-16), the audit log of every admin write (T-17) and the admin's read-only query of the bets placed (T-21).
- `EsquemaBD.md` and `db/init/01-schema.sql` must stay in sync: a schema change updates both in the same change.

- Treat it as the source of truth when adding or changing entities in `src/types/` or `src/data/`, so the static layer keeps converging on the future tables.
- Where the current types differ from the schema (e.g. `Match.homeTeamId`/`awayTeamId` vs. the `partido_equipo` rows), the data layer does the translation. The UI does not change.
- If a change contradicts the schema, update both this section and `EsquemaBD.md` in the same change.

**Engine and conventions**

- **MySQL 8** (≥ 8.0.16 so `CHECK` is enforced), InnoDB, `utf8mb4`.
- Table and column names are `snake_case` Spanish (`partido_equipo`, `es_visita`). TypeScript stays camelCase; the data layer maps between them.
- **Vocabulary:** the tournament uses **puntos** (standings, informational). The pool uses **monedas** (what a user spends to bet, earns on validation) and, separately, its own **puntos** (BR-039: never the same thing as coins). Never mix the terms.
- Ids: `BIGINT UNSIGNED AUTO_INCREMENT`. Dates: `DATETIME`, always UTC (MySQL stores no zone), no `DEFAULT CURRENT_TIMESTAMP` — the backend sets it explicitly. Pool coins and points: whole integers (`SMALLINT UNSIGNED`/`SMALLINT`), never `DECIMAL`/`FLOAT` — the old anticipation-based fractional bonus is gone.
- Catalog tables have a stable unique `codigo`. Logic matches on `codigo`, never on the numeric id.
- **Computed, never stored:** standings, top scorers, a match's general result, the pool ranking, bet closing time. The one deliberate exception is `usuario.saldo_monedas` (must never go negative, and MySQL can't `CHECK` against another table's sum) — kept in sync with `movimiento_moneda` by the backend, in the same transaction.
- **Business rules that matter never live only in the frontend** (project-wide rule for the betting platform).

**Modules.** Informativo must never depend on Polla or Auditoría. When an Informativo rule needs a Polla fact, Informativo declares an extension point (a function type) and the composition root (`server/src/routes/index.ts`) passes Polla's implementation in. Example: `DrawRuleGuard` in `services/sports.service.ts`, implemented by `services/bets-sport-guard.service.ts`.

| Module | Tables |
|---|---|
| Acceso | `rol`, `estado_usuario`, `estado_pago`, `usuario`, `sesion` |
| Informativo | `deporte`, `competicion`, `equipo`, `jugador`, `plantel`, `estado_partido`, `partido`, `partido_equipo`, `gol`, `multimedia_partido` |
| Polla | `tipo_apuesta`, `resultado_general`, `estado_seleccion`, `ticket`, `seleccion`, `tipo_movimiento`, `movimiento_moneda` |
| Auditoría | `accion_auditoria`, `auditoria` |

**Tables at a glance**

- `rol` (codigo: `apostador` = BR-002's "Usuario", `admin`) → `usuario` (rol_id, estado_usuario_id, estado_pago_id, nombre, email, password_hash, saldo_monedas, creado_en). One role per user; roles do not include each other. **Admins never take part in the pool** (user decision, BR-001): they are not participants, never get payment confirmed, validated or coins, and never bet. A user is not a player. `estado_usuario`: `pendiente`/`validado` (BR-005). `estado_pago`: `pendiente`/`confirmado` (BR-006).
  - **Login is by email only**; there is no separate username (BR-003/BR-004 say "usuario o correo", and `business-rules.md` now says email). `email` is stored trimmed and lowercased; `nombre` is display-only and may repeat. `password_hash` is argon2id.
  - API registration always creates `apostador` + `pendiente` + pago `pendiente` + 0 coins. `nombre` follows the catalog's `displayName` rules (D-011: a letter or digit, no control or invisible characters). An `admin` only comes from the server-side `admin:create` command.
- `sesion` (usuario_id, token_hash, creado_en, expira_en, indexed) is one server-side login session (NFR-005). It stores only the SHA-256 of the cookie token, never the token. A session is live while `expira_en > now`, and logout deletes the row. `ON DELETE CASCADE` from `usuario`.
- `deporte` (nombre, slug, permite_empate) and `competicion` (deporte_id, nombre, slug) — BR-011 splits these; a competición is one standalone tournament, no seasons.
- `equipo` (competicion_id, nombre, nombre_corto, escudo, color_acento). A team belongs to exactly one competición.
- `jugador` is the person. `plantel` (jugador_id, equipo_id, competicion_id, numero_camiseta) enrolls that person in a team. `UNIQUE(jugador_id, competicion_id)` means one team per competición and no mid-tournament transfers.
- `partido` (competicion_id, estado_partido_id, jornada, fecha_hora, sede). States: `programado`, `en_curso`, `finalizado`, `cancelado` (BR-012). A match starts **by itself** at `fecha_hora` and lasts 60 minutes; the stored state may still say `programado`, so always use the effective state (`lib/match-state.ts`, EsquemaBD D21). "Deporte" (BR-011) comes via `JOIN` through `competicion`, never duplicated. Goals and the general result are never columns here (see `partido_equipo` and Business rules).
- `partido_equipo` (partido_id, equipo_id, es_visita, goles) holds **exactly two rows per match**, local (`es_visita = false`) and away. Goals are entered by hand by an admin (BR-028), locked once the match is `finalizado`.
- `gol` (partido_equipo_id, plantel_id, equipo_id, minuto 1–120, imagen?, video?) — a goal's scorer, team and minute, with optional media (BR-033). `imagen` is a server-made file name (32 hex + `.webp`) in `UPLOADS_DIR`, never a URL; `video` is a normalized YouTube/Vimeo link.
- `multimedia_partido` (partido_id, imagen? | video?, creado_en) — the match's own images and video links (T-13, BR-001), exactly one of the two per row. Replaces the old generic `partido_jugador`/`estadistica_tipo`/`partido_jugador_estadistica`, which no BR needs (the frontend's player radar stats are unrelated random data, see above).
- `tipo_apuesta` (codigo: `resultado_general`, `marcador_exacto` — BR-015/BR-016). `resultado_general` catalog (codigo: `local_gana`, `empate`, `visitante_gana` — BR-029) is reused both as a `seleccion`'s pronóstico and as a match's derived result.
- `ticket` (usuario_id, creado_en, clave_idempotencia, huella_solicitud) groups one or more `seleccion` rows (BR-019). Coins used, points and the ticket state are always computed from its selections, never stored on the ticket. `UNIQUE(usuario_id, clave_idempotencia)` is BR-054's barrier (EsquemaBD D20).
- `seleccion` (ticket_id, partido_id, tipo_apuesta_id, pronostico_resultado_id?, pronostico_goles_local?, pronostico_goles_visitante?, estado_seleccion_id, puntos_obtenidos?) is one individual bet. `CHECK` enforces exactly one pronóstico shape is filled; matching it to `tipo_apuesta` by `codigo` is backend logic. States: `pendiente`, `acertada`, `no_acertada`, `anulada` (BR-027). Several selections per match and per ticket, even contradictory ones, are allowed (BR-017/BR-018) — no uniqueness constraint groups them. The 1-coin cost per selection (BR-020) is a backend constant, never a column.
- `tipo_movimiento` (codigo: `validacion` +10, `seleccion_confirmada` −1, `devolucion_cancelacion` +1 — the only tabla-28 events that move coins) → `movimiento_moneda` (usuario_id, tipo_movimiento_id, seleccion_id?, cantidad con signo, creado_en). `UNIQUE(seleccion_id, tipo_movimiento_id)` stops the same selection from being processed twice under the same movement type. A generated column `sin_seleccion` (1 when `seleccion_id` is NULL) plus `UNIQUE(usuario_id, tipo_movimiento_id, sin_seleccion)` allows at most one selection-less movement per user and type, which makes the `validacion` +10 unique at the database level (BR-008, EsquemaBD D19). Revisit that index if a repeatable selection-less movement type is ever added.
- `accion_auditoria` (codigo, nombre, entidad — the table each code affects) → `auditoria` (usuario_id = admin, accion_id, entidad_id, creado_en, detalle JSON of at most 4 KB). The catalog has the 5 NFR-006 codes plus, since T-17, one per admin write (payments, and create, edit and delete of matches, sports, competitions, teams, players, enrollments, goals and match media) and the two of `admin:create` (`creacion_administrador`, `promocion_administrador`, D-005). `entidad_id` has **no FK** (it points at different tables depending on `accion_id`, and MySQL can't express a conditional FK) — the backend owns that integrity.
- Composite FKs over `(id, competicion_id)` and `(id, equipo_id)` guarantee teams, matches, players and goals share a competición. See `EsquemaBD.md`.

**Business rules (backend)**

- Standings **puntos** (informational, per competición): win **3**, draw **1**, loss **0**. Unrelated to the pool's own points.
- The pool: monedas fund bets (BR-008 to BR-010), puntos measure performance (BR-039) — never convert one into the other.
- A `seleccion` closes **24 h before** `partido.fecha_hora`, and only while the match is `programado` (BR-014). A match created less than 24 h ahead is born closed.
- A match registered before it is played can get its result, goals and media **after** its date (user decision): T-12/T-13 must not require a future date.
- Points per selection, evaluated independently when the match's result is confirmed (BR-034 to BR-038):

  | Bet type | Condition | Points |
  |---|---|---|
  | `resultado_general` | Correct winner | +3 |
  | `resultado_general` | Correct draw | +1 |
  | `marcador_exacto` | Correct exact score | +3 |
  | Either | Wrong | 0 |

- Confirming a result (admin action) moves the match to `finalizado`: that transition **is** the lock (BR-031/BR-032) — goals and state become immutable in the app from then on, no separate "confirmed" column. The system then settles every `seleccion` for that match per the table above.
- Cancelling a match (`cancelado`, final) voids every one of its `pendiente` selections (`anulada`, points NULL) and refunds 1 coin for each debited one of an `apostador` account (never an admin's, D-002 in `docs/decisiones.md`) via a `devolucion_cancelacion` movement, updating the balance in the same transaction (BR-045 to BR-047, BR-055). Other selections in the same ticket, from other matches, are untouched; settled or already voided ones (hand-loaded data only) too. A cancelled match can be deleted only with no bets, goals, result or media (D-001). "Coins refunded" on receipts, my bets, its summary and the pool figures is the sum of the real refund movements, never a count of voided selections (D-003).
- Validating a user (`estado_usuario → validado`) grants +10 coins exactly once (BR-006 to BR-008). The backend allows only the transition from `pendiente` with payment `confirmado`, in a conditional UPDATE in the same transaction as the movement, and the database refuses a second `validacion` movement (D19). The amount lives only in `server/src/lib/coins.ts`.
- Participation flow (§23): confirm payment, then validate. A payment can be reverted only while the user is still `pendiente`. Validation is never reverted. These actions only apply to `apostador` accounts; on an admin account they return 404 `NOT_A_PARTICIPANT`. The participant table and counts show **only apostadores**.
- **Every pool query excludes admins** (`rol = 'apostador'`): participant table, counts, ranking (T-15), pool statistics and bet listings (T-21).
- Pool ranking (T-15): only validated `apostador` accounts, 0 included. `SUM(seleccion.puntos_obtenidos)` per user, ordered by `puntos DESC, aciertos DESC`; a full tie shares its position and the next one skips (1, 1, 3); name (Spanish order: `utf8mb4_es_0900_ai_ci` in SQL, `Intl.Collator('es')` in TypeScript) then id only order a tie for display. The top is every position from 1 to 10, so ties at 10 all get in, capped at 50 rows with `topSinMostrar` counting the rest (BR-041 to BR-044).
- **Resolved:** `resultado_general`'s "empate" is gated by `deporte.permite_empate` (BR-015, confirmed) — not enforced by the schema itself, applied in backend logic when betting rules are built (T-09). BR-007's admin table shows only `estado_pago` and `estado_usuario`, no separate "Estado" column.
- **Resolved (T-10):** BR-054's idempotency is a client `Idempotency-Key` UUID stored on the ticket (EsquemaBD D20).

### Change history

[historial.md](historial.md) (in Spanish) is the log of **tested and reviewed changes**. A change is recorded only after `tester_liga` has verified the work of `ejecutor_liga` and approved it. Each entry states the date, what changed, the files and how it was verified.

- Read it to learn which changes are already verified before building on them.
- Only `tester_liga` appends entries, at the end of the file. Never rewrite or delete past entries.
- **Report back to the coordinator (Herdr pane `w4:p1`) with `herdr agent prompt w4:p1 "<summary>"`**, no double quotes inside the text. `ejecutor_liga` reports when it finishes a task or a fix, or when it is blocked on a decision; `tester_liga` reports every verdict, pass or fail. The coordinator relies on these messages to hand work over.
- **Two testers work in parallel:** `tester_liga` (pane `w4:p3`) and `tester_liga_2` (pane `w4:p5`, its own tab). The coordinator may split one task between them; each verifies only its part, and only the one told to close the task ticks `docs/plan-polla.md` and writes `historial.md` (re-read both before writing). To avoid clashing:
  - Test database: `la_liga_acp_test` for `tester_liga`, `la_liga_acp_test_2` for `tester_liga_2` (set as `MYSQL_DATABASE_TEST` in its tab's environment; never change it).
  - Local backend instances: ports 3950–3999 for `tester_liga`, 3900–3949 for `tester_liga_2`. Never 3001 or 5173.
  - Never run `docker compose down -v`, or stop/recreate `db` or `server`, unless the coordinator explicitly asks. To test a database outage, point a local instance at a closed port.
  - Test data in the development database carries a per-tester prefix (`t1_`, `t2_`); each tester deletes only its own.
  - **The development database has one owner at a time** (D-020): `ejecutor_liga` while it implements or fixes a task — it cleans its own data and the dev seed *before* reporting the task done — and, from that report until the task is closed, the tester doing the browser review, which loads and removes its own seed data. Nobody else runs `server:seed:dev`/`:clean` against it in the meantime; automated tests always use each agent's own database.
- **Context near capacity:** before the coordinator sends a new instruction to any pane, it checks that agent's context. If it is near capacity (≥ 80% of the window, or Claude Code shows "% until auto-compact"), the coordinator runs `/clear` on that pane (never mid-task), re-sends the agent's role, rules and current goal, and only then the instruction. An agent that receives a message starting with "CONTEXTO REINICIADO" must re-read the listed files before doing anything else.

### Coordinator decisions

[docs/decisiones.md](docs/decisiones.md) (in Spanish) logs every question the coordinator resolved on the user's behalf while running the task loop (question, options, decision, reason). Only the coordinator writes it. Agents read it to apply those decisions, and never edit it.

### Final review (T-23)

[docs/verificacion-final.md](docs/verificacion-final.md) (in Spanish) is the rule-by-rule check of the finished system: each of the 55 BR and 6 NFR with where it is met, how it is verified and its state. Update it when a rule's implementation moves.

### Backlog

[docs/pendientes.md](docs/pendientes.md) (in Spanish) lists known issues **deliberately postponed**: Astro-parity gaps left by the React migration, pixel-art rules the CSS still breaks, doc cleanup and open schema decisions.

- Nothing there is in progress. Work on an item only when asked for it.
- Add an item when something is found and postponed; tick it off when it ships, and record the change in `historial.md`.

### Assets

Team crests and logos live in `src/assets/` and are imported with a rendition preset, `?pixel=<preset>` (e.g. `import crest from '../assets/boca.avif?pixel=crest'`), then rendered through `<PixelImage />` — not referenced from `public/`.

- `vite-plugins/pixel-images.ts` builds small webp renditions with sharp, byte-identical to what `astro:assets` produced. Presets live in `vite.config.ts`; each spec is a width (`"96"`), a crop (`"32x32"`) or a density of either (`"96@2"`).
- `<PixelImage image width height? densities? />` picks the 1x rendition, adds the `srcset`, and sets `width`/`height` from the real file. Asking for a size that isn't in the preset throws: add it to the preset instead of loading the original.
- Declare every new preset in `src/vite-env.d.ts` so the import is typed.

## Stack

- **Vite + React + TypeScript**, React Router in data mode (`createBrowserRouter` in `src/App.tsx`). Entry: `index.html` → `src/main.tsx`.
- **Routes:** `/` (`src/pages/Home.tsx`), `/posiciones` (`Posiciones.tsx`), `/plantilla/:id` (`Plantilla.tsx`), and the account pages of T-18: `/ingresar` (`Ingresar.tsx`, sign in, with the front's own `?next=`), `/registro` (`Registro.tsx`), `/cuenta` (`Cuenta.tsx`, needs a session); the admin panel of T-21 (`src/pages/admin/`, admin role): `/admin` and its sections `participantes`, `partidos`, `partidos/:id`, `apuestas`, `ranking`, `auditoria`, `deportes`, `competiciones`, `equipos`, `jugadores`, `planteles`; and the betting pages of T-19: `/apuestas` (`Apuestas.tsx`, `apostador` role) and `/apuestas/tickets/:id` (`Ticket.tsx`, the receipt); and the pages of T-20: `/mis-apuestas` (`MisApuestas.tsx`, `apostador` role) and `/ranking` (`Ranking.tsx`, any session). An unknown team id throws a 404 from the loader; unknown URLs hit the `*` route. Both render `NotFound.tsx` inside the layout; its `RouteError` also shows a 403 and an unreachable API.
- **Layout:** `src/layouts/Base.tsx` (backdrop, navbar, `<main>`). The `<head>` lives in `index.html`; each page sets its title with `useDocumentTitle()`. The navbar has two rows below 64rem (site links, then `SessionBar`: sign in / sign up, or coins + account + sign out, or admin + sign out) and one from 64rem; `--navbar-height` includes both rows.
  - **Two colors, one rule (C-02).** The **landing's sections** — Inicio, Fixture, Posiciones, in `Navbar.module.css` — keep the normal color (`--color-text-muted`, lighting up to `--color-accent`). **Everything of the pool or the account** — Polla and its links, Mi cuenta, Salir, Ingresar, Crear cuenta, Admin, Ranking, in `SessionBar.module.css` — is **gold** (`--color-accent-alt`, lighting up to `--color-coin-light`), so it never reads as a section of the site. A new link goes in the file of the group it belongs to and inherits its color; never write the gold as a hex. The page on screen is still told by `aria-current` and its own marker, never by color alone. `src/components/navbar-colors.test.tsx` keeps the two groups from mixing.
- **API client (T-18, D-006 in `docs/decisiones.md`):** the front calls the API on its own origin under `/api` (Vite's proxy in `vite.config.ts` forwards to `API_PROXY_TARGET`, default `http://localhost:3001`, dropping the prefix; production needs a reverse proxy, see the README's deployment section). **`src/lib/api.ts` is the only code that calls `fetch`**: JSON, the `{ data }`/`{ error }` envelope as `ApiError`, the CSRF token kept in memory and sent on writes (renewed once through `/auth/me` on `CSRF_FAILED`), 401 (other than `INVALID_CREDENTIALS`) as an expired session, and URLs only from a fixed path plus an explicit `query` object: never the page's own parameters. **A 2xx that doesn't carry the `{ data }` envelope** (a proxy serving `index.html` for `/api`, a broken or truncated body, a bare array, a `null`) is an `ApiError` with **status 0** and code `BAD_RESPONSE`, so `isTransientError` catches it and every page shows its notice with "Reintentar" instead of an error page titled "Error 200" (T-22 second fix). Statuses outside 2xx keep their own status and code. API contracts live in `src/types/api.ts`.
- **Session (T-18, D-009):** `src/lib/auth.ts` holds it in memory (login, register, logout, `refreshSession`, `readSession`, `cachedSession`, `refreshCoinBalance`), read by `useSession()`. Protected pages (and `/ingresar`, `/registro`) always read `/auth/me` in their loader (`readSession`) and render only the live store user, never loader data. Public pages use the in-memory copy while it is younger than `SESSION_CACHE_MS` (60 s), so browsing doesn't spend the backend's limits; anything that spends coins (T-19) calls `refreshSession()` afterwards. Any 401 empties the store; the layout then sends a protected page to `/ingresar?next=`, with the URL the user asked for, or re-runs its loader on a role change. **Every way of asking for a page records its destination the moment it is asked for** (`src/hooks/useRequestedPath.ts`): a link, through the click on it; a page's own navigation (a filter, "Quitar filtros", the page URL a list replaces), through `useRememberedNavigate`, which `/mis-apuestas`, `/apuestas`, the panel's `FilterForm` and `usePageUrlFix` use instead of `useNavigate`; and the navigation React Router already has in flight, which wins while it lasts. Without that record the redirect used the URL still on screen and the filter or page just asked for was lost. Everything recorded passes `safeNextPath`, is forgotten once that page is on screen, and expires in two seconds. The layout passes the session to the navbar as props. Protected routes use the loaders in `src/lib/route-guards.ts` (`requireUser`, `requireRole`, `requireKnownUser`, `guestOnly`, `isProtectedPath`); they are a convenience, the backend checks again. A page that reloads in place (`/mis-apuestas`, `/ranking`) uses `requireKnownUser`: a session that can't be checked right now (no network, 5xx, 429) with a user already known in the tab keeps the page and what it shows, with its notice and "Reintentar" (T-20 fix); `/apuestas` keeps its list the same way while one is on screen. With no known user it is the error page, whose "Reintentar" re-runs the loaders; a 401 still goes to sign in. `?next=` goes through `safeNextPath` (`src/lib/next-path.ts`), which checks the raw text **and** the normalized path (dot segments can turn `/.//host` into `//host`): in-app paths only.
- **Landing after sign-in (D-008):** without `?next=`, a validated participant goes to `/apuestas`, a pending one to `/cuenta`, an admin to `/admin` (`homeFor`). A session read that fails with a 429 or no network is an unknown state, never shown as signed out.
- **Betting UI (T-19):** `src/lib/betting.ts` is its data layer (list, sports, preview, confirm, receipt, and the page-URL filters `deporteId`, `desde`, `hasta`, `estadoApuesta`, `page`, with days in Lima time). `/apuestas` loads in its loader and previews and confirms through its route `action` (JSON fetchers; a preview skips revalidation). `BetMatchCard` shows each match with a `PixelIcon` + label per betting state (never color alone) and the forecast controls only when `disponible`; `TicketPanel` is the ticket being built (bottom bar on phones, sticky column from 64rem, `aria-live` announcements). The draft lives in `sessionStorage` per user (`src/lib/ticket-draft.ts`, D-012), removed on confirming, emptying or signing out (a session that just expires leaves it in that tab, visible only to the same user). `loadDraft` validates all of it (match data, the selection by type with only its fields via `selectionInputOf`, ids, goals, a UUID key) and drops what fails, never the page; a render error in the panel stays inside `RenderGuard`. The panel refreshes each match from the list and the preview. A confirmation that fails with `RATE_LIMITED`, `NETWORK_ERROR` or `TIMEOUT` skips revalidation, and a failed list reload with a draft keeps the last list and the ticket. Every change of the selections gets a new `Idempotency-Key`; a retry of the same selections reuses it. After confirming, `refreshSession()` updates the coin counter and the receipt opens. Crests from the API go through `TeamCrest` (`<img>` only, root-relative paths, `pixelated`).
- **Bets history and ranking (T-20):** `src/lib/bet-history.ts` (list, summary, competitions, the page-URL filters `estado`, `estadoTicket`, `deporteId`, `competicionId` (only with its sport), `desde`, `hasta`, `page`, and `groupByTicket`) and `src/lib/ranking.ts`. Both pages read in their loader on every visit; a transient failure (`isTransientError` in `api.ts`: no network, 429, 5xx) stays on the page with "Reintentar" and keeps what it showed. Focus: "Anterior"/"Siguiente", "Filtrar", "Quitar filtros" and a "Reintentar" that loads move it to the results count (announced), or to the notice if the load failed; Back/Forward never move it (the browser owns history focus). `?page=` past the end shows the last page and replaces the URL (`shouldRevalidate` skips that reload). Buttons that load use `aria-disabled`, never `disabled`, so the focus stays. The ranking is a `<table>` (short headings on phones, full words for screen readers), shared positions marked with `=`, the own row with a "TÚ" tag, apart after the list when `enLista` is false, and `topSinMostrar` said in words. Filters of free-text names (sports, competitions) are `ChoiceGroup` chips (D-017), states are `StateTag` (icon + word). The session bar gives a participant a "Polla" disclosure menu (Apostar, Mis apuestas, Ranking) and an admin a Ranking link.
- **Coin counter (BR-010):** `SessionBar` shows it to every `apostador` (a pending one with 0 and a "Pendiente" tag), never to an admin. The coin is `CoinIcon.tsx`, an 8×8 sprite drawn with hard `box-shadow` pixels on `--coin-px` (colors `--color-coin-*` in `global.css`), no image or emoji.
- **Front tests:** `npm test` (Vitest + Testing Library on jsdom, `vitest.config.ts` merges the Vite config, only `src/**/*.test.{ts,tsx}`). `fetch` is mocked (`src/test/fetch-mock.ts`), pages run in a memory router (`src/test/render-routes.tsx`). Every new front feature gets tests there. **A simulated API answers what the real one answers, and 404s anything it wasn't asked to simulate** (`apiRoutes`): its shapes are the backend's own (the sport travels beside the competition inside a match or a team, never nested; a finished match carries its derived `resultado`; a ticket preview carries each selection's match) and a route with filters applies them — `matchesRoute` filters (`competicionId`, `deporteId`, `estado`, `jornada`), orders by proximity (BR-013) and paginates like `/public/partidos`, and `competitionsRoute` filters by `deporteId`. A handler that replies the same thing whatever is asked hides the defect it should catch.
- **Styles:** `src/styles/global.css` is imported first in `main.tsx`. Each component has a `Component.module.css` next to it. Global primitives (`pixel-box`, `pixel-bevel`, `pixel-shadow`, `pixelated`) are plain class names; everything else goes through the module.
- **Scrolling:** `useScrollManagement()` (in the layout) keeps multi-page behavior: a new route starts at the top or at its `#anchor` instantly, a same-page anchor scrolls with the CSS behavior, and Back/Forward and reload restore the offset.
- **Admin panel (T-21):** `src/pages/admin/`. `routes.tsx` is the route tree (`adminRoute`, used by `App.tsx` and the test routers): `AdminLayout` adds the panel's navigation (a wrapping list of links, only for an admin) and every section is `lazy` (its own chunk). Each section's loader goes through `loadAdmin` (`src/lib/admin-load.ts`: `requireKnownUser(args, 'admin')`, a transient failure keeps the page with its notice and "Reintentar"). Data layer: `src/lib/admin-core.ts` (URL filters with the API's names, league-time dates `leagueInputOf`/`isoFromLeagueInput`, `perform`, which turns a write into an `ActionOutcome` with the backend's message plus a hint per code and the per-field errors), `admin-catalog.ts` (catalog, matches, result, goals, media, cancellation) and `admin-pool.ts` (participants, bets, ranking, figures, audit); types in `src/types/admin.ts`. Writes go through each route's `action` with fetchers; one fetcher per page (or per form) keeps its message when a row leaves the list. Image uploads are the only multipart request (`api.upload`); the form checks the file and its 5 MB first. Images show only from `/api/admin/archivos/<32 hex>.webp` (`adminImageSrc`) and videos only through `embedUrl` (`safeEmbedUrl`) in a sandboxed iframe. Irreversible actions (confirm a result, cancel, delete, removing a goal's image or video) and participant actions use `ConfirmStep` (`src/components/admin/AdminUi.tsx`, with the shared `SelectField`, `ActionMessage`, `DataTable` that stacks on phones, `FilterForm`, `Pager` and `LoadNotice`). The panel never changes roles. A match that was deleted isn't read again (`shouldRevalidate`): the list opens with the message.
- **Choosing a record in the panel (D-019):** a competition, a team, a player or a squad is chosen with `SearchSelect` (`src/components/admin/SearchSelect.tsx`), a combobox with a listbox: it asks the API for the options that match what is typed (`catalogSource` in `src/lib/admin-choices.ts`, one page at a time, "Ver más opciones" for the next), shows the chosen one whole in as many lines as it takes, and is driven by the arrows, Enter and Escape with the active option in `aria-activedescendant`. A native `select` can't do either thing: it only offered the first page (the 101st record was unreachable) and it cut long names at 320 px (D-014). A fixed list with long names (the audit actions) uses the same control with `fixedSource`; short fixed lists stay a `SelectField`. Each row names what it points at from the API's own joined fields (`competicionNombre`, `equipoNombre`, `deporteNombre`), never from a list of options, and a filter's id in the URL reads its name with `labelById`. Every admin list says when `?page=` is past the end, shows the last one and replaces the URL with it without reading again (`pageInRange`, `usePageUrlFix` and `skipPageFix`, as `/mis-apuestas` does), and its "Filtrar" moves the focus to the results, or to the notice when the load failed. The chooser closes its list when the focus leaves the field, and never opens it just because the field has the focus.
- **Static hosting:** the app's deep routes (`SPA_ROUTES` in `vite.config.ts`: `/posiciones`, `/plantilla/:id`, `/ingresar`, `/registro`, `/cuenta`, `/admin` and each admin section (`/admin/partidos/:id` included), `/apuestas`, `/apuestas/tickets/:id`, `/mis-apuestas`, `/ranking`, each with and without a trailing slash, `:id` = one path segment) are rewritten with 200. There is never a `/*` catch-all, so real files keep their response and unknown URLs get 404.
  - `vite-plugins/spa-rewrites.ts` generates `dist/_redirects` at build time. The target is `/` (Cloudflare Pages rejects `/index.html` as an infinite loop) unless the build runs on Netlify (`NETLIFY=true`), which gets its documented `/index.html`. Don't add a static `public/_redirects`.
  - `vercel.json` holds the same rewrites for Vercel. The build fails if it doesn't match `SPA_ROUTES`. **Adding a route in `src/App.tsx` means adding it to `SPA_ROUTES` and `vercel.json`.**
  - `vite build` also writes `dist/404.html` (a copy of `index.html`): the body hosts send for unknown URLs, and the only fallback on hosts without rewrites, where deep links render but return 404.
  - Verify Cloudflare behavior with `npx wrangler pages dev dist` (no invalid-rule warnings). `vite dev` and `vite preview` serve deep links, with or without a trailing slash, with 200. Details in the README's deployment section.

## Backend stack

- **Express 5 + TypeScript**, its own `server/` project with its own `package.json` — not an npm workspace, see `server/README.md` for why. Layers: `routes` (URL + verb only) → `controllers` (HTTP shaping) → `services` (business logic + data access) → `db/pool.ts` (mysql2/promise pool). The `pool` is threaded down as a parameter from `app.ts`, never a module-level singleton — that's what lets tests inject their own (including one pointed at an unreachable host, to exercise failure paths for real).
- **Config:** `server/src/config/env.ts` validates every environment variable with `zod` in `loadEnv()`, called once at startup by `index.ts`; the process refuses to start rather than run half-configured, printing every bad/missing variable and exiting with code 1 (no stack trace). `MYSQL_DATABASE_TEST` must differ from `MYSQL_DATABASE`. Nothing else reads `process.env` directly. Reuses the root `.env`/`.env.example` (same file `compose.yaml` uses for `db`) — see it for the full variable list, including `DB_HOST`/`DB_PORT` (differ between bare local dev and the `server` compose service) and `MYSQL_DATABASE_TEST`.
- **Validate every input, including the query string.** Bodies, `:params` and query go through zod. Query schemas are `z.strictObject`, and a route that takes no query uses `rejectQueryParams` (`middleware/no-query.ts`, after `requireAuth`). No route under `/auth` or `/admin` may silently ignore an unknown parameter: it's a 400. `/health` is the deliberate exception.
- **Response envelope:** every response is `{ data }` or `{ error: { code, message, details? } }` — success and error alike, including `404`s and validation failures. Controllers/services `throw new HttpError(...)` (`server/src/lib/http-error.ts`) or let a `ZodError` propagate; Express 5 forwards a rejected async handler to the error middleware automatically, no manual `try/catch`/`next(err)`. Any error with `expose: true` and a 4xx `status` (what `express.json()` raises for a bad body) is a client error, not a crash: it keeps its status, isn't logged, and never forwards the library's message. Known body-parser `type`s get their own code — `INVALID_JSON` (400), `PAYLOAD_TOO_LARGE` (413), `UNSUPPORTED_MEDIA_TYPE` (415) — and the rest, including a corrupt gzip/brotli body (no `type`), get `BAD_REQUEST`. A 5xx or non-exposed error is always `INTERNAL_ERROR`. Unexpected errors always log the real cause server-side and return a generic `INTERNAL_ERROR` — never a stack trace or driver message to the client.
- **`GET /health`** checks the database live (with one retry) on every call — it does **not** gate process startup; the app comes up even with the database down, so `/health` itself can report that clearly (`503 DATABASE_UNAVAILABLE`) instead of the whole process being unreachable.
- **Security base (NFR-005):** `helmet()`, CORS locked to `CORS_ORIGIN` (no wildcard), a basic global rate limit (`GET /auth/me` is outside it with its own `SESSION_READ_RATE_LIMIT_*`, D-009; every 429 carries `details.limite`), then a 100kb JSON body limit, all wired in `server/src/middleware/security.ts`. Image uploads (T-13) have their own multipart parser, size limit and rate limit (`middleware/upload.ts`, `uploadRateLimit`). The limiter runs before body parsing, so invalid bodies count too, and skips `GET /health` with the same matching the route uses (case-insensitive, optional trailing slash).
- **Tests:** Vitest + Supertest, against `MYSQL_DATABASE_TEST` — never the real database. `server/tests/global-setup.ts` recreates and migrates it from `db/init/` once per run (as `root`, since the app's own MySQL user only has grants on `MYSQL_DATABASE`), so it can't drift from the real schema. Test files run serially (`fileParallelism: false`): they share one database. `resetDatabase()` (`server/tests/helpers/db.ts`) empties every table except the catalogs loaded by `02-catalogos.sql`, for use in `beforeEach`. It uses `DELETE` (`TRUNCATE` made each reset take ~1 s), so AUTO_INCREMENT keeps counting and tests must never assume specific ids. `server/tests/helpers/auth.ts` has the register/login/role shortcuts.
- **Never let tests touch the real database.** `vitest.config.ts` forces `NODE_ENV=test` (a value exported in the terminal can't override it). `global-setup.ts` and `resetDatabase()` refuse to run unless their target is exactly `MYSQL_DATABASE_TEST` (`server/tests/helpers/test-database.ts`). Keep all three guards.
- **Auth (T-03, details and rationale in `server/README.md`):**
  - Server-side sessions, not JWT, so logout really invalidates. The cookie (`HttpOnly`, `SameSite=Strict`, `Path=/`, plus `Secure` and the `__Host-` prefix in production) expires after `SESSION_TTL_HOURS`. The user is re-read from the database on every request, so role and state changes apply immediately.
  - Passwords use argon2id (19 MiB, t=2, p=1). A failed login always returns the same `401 INVALID_CREDENTIALS`, and an unknown email still runs a dummy verification so both cases take about the same time.
  - **A password being chosen is 6 to 20 characters and nothing else** (BR-003, C-01): no uppercase, digit or symbol is required and nothing is refused for being weak. `PASSWORD_MIN_LENGTH`/`PASSWORD_MAX_LENGTH` (`lib/password.ts`) are the only place with the numbers, `newPasswordSchema` the only check, and both registration and `admin:create` use it. Length is counted in **code points**, not UTF-16 units, so a plain emoji counts once — a composed one (a ZWJ family, a flag) counts as many as it is made of, and so does a letter written with a separate combining accent.
  - **Signing in never applies that rule** (D-024): an account created before C-01 may hold a longer password and has to get in, and refusing by length would hint at what is stored. Only `PASSWORD_VERIFY_MAX_LENGTH` (128) bounds what is hashed, and going over it fails exactly like a wrong password — same 401, same message, same time (the dummy verification runs anyway). The login form sets no `maxLength` either.
  - `/auth/login` has its own limit: `LOGIN_RATE_LIMIT_MAX` failed attempts per IP + email. `/auth/register` has another one: `REGISTER_RATE_LIMIT_MAX` attempts per IP, successful or not. The user decided the duplicate email keeps answering 409 `EMAIL_TAKEN`.
  - Rate limits key on `req.ip`, which is the socket address unless `TRUST_PROXY` (off by default; a hop count or IPs/CIDRs, never `true`) names the proxies allowed to set `X-Forwarded-For`. It is applied in `app.ts`.
  - `SESSION_SECRET` rejects placeholder-looking values, including the one in `.env.example`. Every login also purges expired sessions of all users, via `idx_sesion_expira_en`.
  - **CSRF** (`middleware/csrf.ts`) applies to every non-GET request. A foreign `Origin` gets 403. With a session cookie, `X-CSRF-Token` must equal the `csrfToken` returned by login and `/auth/me` (an HMAC of the session token with `SESSION_SECRET`). Login and register are exempt from the token, not from the Origin check.
  - **Route protection:** `requireAuth` (401) → `requireRole(role)` (403, exact role; roles don't include each other) / `requireParticipant` (403 `NOT_A_PARTICIPANT`: any `apostador`, for their own pool data) / `requireBettor` (403). Everything under `/admin` already has `requireAuth` + `requireRole('admin')`.
  - **Every betting or coin-spending route (T-09 on) must use `requireAuth, requireBettor`.** Read-only pool routes use `requireParticipant` instead (the match list since T-19, the history, `/monedas`). It lets through only a validated `apostador`: an admin gets 403 `ADMIN_CANNOT_BET` even if the database says `validado`, and a `pendiente` user gets 403 `USER_NOT_VALIDATED` (they can still log in and browse, BR-005).
  - **Roles are never changed from the app** (user decision, BR-001). There is no admin panel or API action for it, and T-21 must not add one. An admin is only created or promoted with `npm run admin:create` on the server. Promotion is refused for any account that took part in the pool: not `pendiente`, payment confirmed, coins, movements or tickets.
  - That command takes `ADMIN_EMAIL`/`ADMIN_NOMBRE` from the environment and **prompts for the password without echo** (`docker compose exec -it ...`). Never document or use a password typed on the command line (`ADMIN_PASSWORD=...`, `-e ADMIN_PASSWORD=...`): it ends up in shell history and in docker's visible process command line. For non-interactive use there are `ADMIN_PASSWORD_FILE`, `ADMIN_PASSWORD_STDIN=1`, or `ADMIN_PASSWORD` only when injected by a CI secret store.
- **Coins (T-05, details in `server/README.md`):**
  - **`services/coins.service.ts` is the only code that writes `usuario.saldo_monedas` or `movimiento_moneda`.** Use `applyCoinMovements(conn, userId, movements)` inside the caller's `withTransaction`, or the shortcuts `grantValidationCoins` (T-04), `debitSelections` (T-10), `refundSelections` and, for many users at once, `refundSelectionsBatch` (T-16, a few statements per batch).
  - They take a `TransactionConnection` (`db/transaction.ts`), which only `withTransaction` hands out: calling them outside a transaction doesn't compile and is rejected at runtime. Use this branded type for any future function that must not run in autocommit.
  - It locks the user row (`FOR UPDATE OF u`), refuses a negative balance (409 `INSUFFICIENT_BALANCE`, nothing written), inserts the movements and sets the new balance in the same transaction.
  - Callers pass a movement type, never an amount: amounts and signs live in `lib/coins.ts`. D19 is checked first: `validacion` never has a `seleccionId`, debits and refunds always have one, and it must belong to that user.
  - A repeated movement gets 409 `MOVEMENT_ALREADY_APPLIED` and the whole batch rolls back.
  - A refund needs that user's `seleccion_confirmada` for the selection and no earlier refund (BR-046). Otherwise it gets 409 `SELECTION_NOT_DEBITED` or `MOVEMENT_ALREADY_APPLIED`, and nothing is written.
  - The participant's own `GET /monedas/saldo` and `GET /monedas/movimientos` (paginated, newest first) use `requireParticipant`: an admin gets 403 `NOT_A_PARTICIPANT`. `npm run coins:check` and `GET /admin/monedas/consistencia` report `saldo_monedas <> SUM(cantidad)` and admins with coins, read-only.
- **Sports catalog (T-06, details in `server/README.md`):**
  - Admin CRUD under `/admin/{deportes,competiciones,equipos,jugadores,planteles}`: paginated lists with filters, read, create (201), PATCH, delete. Strict zod for body and query.
  - Slugs come from the name when not sent and are always normalized (`lib/slug.ts`). A taken one is 409 `SLUG_TAKEN`.
  - Every list takes `q` (planteles too, by player or team name since T-21), and its rows carry the names they are read with, joined, never stored: `competicion.deporteNombre`, `equipo.competicionNombre`/`deporteNombre`, `plantel.equipoNombre`/`competicionNombre`/`deporteNombre`, `partido.competicionNombre`/`deporteNombre`. `soloGuardados` keeps them out of the audit detail.
  - `page` and `pageSize` are whole numbers written only with decimal digits in every paginated list (`pageNumber`): `1e2`, `0x10`, ` 2` and `2.0` are 400, each with its own message.
  - Deletes are refused while anything uses the row (409 `*_IN_USE` with counts). No cascade, no soft delete.
  - Plantel: the competition always comes from the team. A different one sent by the client is 409 `COMPETITION_MISMATCH`, and changing team or player is 409 `TRANSFER_NOT_ALLOWED` (D4).
  - `permite_empate` only changes while no match left `programado` and no bet exists: 409 `DRAW_RULE_LOCKED`.
  - Names (`displayName` in `schemas/catalog.schema.ts`) reject control characters, the whole Unicode Cf category except ZWJ/ZWNJ (bidi, zero-width, BOM, soft hyphen, tag characters…), line/paragraph separators, blank-looking fillers and lone surrogates, and must contain at least one letter or digit. Slugs transliterate letters that have no accent decomposition (ß→ss, æ→ae, ł→l…).
  - Crest and photo: an `https://` URL or a relative image path, up to 255 characters, with no lone surrogates, control, format, line-separator, blank-looking or space characters (400). Color: `#rrggbb`. The server must never fetch those URLs without a host allowlist (any https host is accepted today, including internal ones). The frontend shows them only with `<img>`/`<PixelImage>`, never as inline SVG or in any other context.
  - Every write goes through `runAdminAction` (`services/admin-action.ts`), whose `hooks.inTransaction` writes the audit record (T-17). Its work may return a `detail` for the record, and it takes the transaction options (isolation).
- **MySQL constraint errors are translated in one place: `lib/db-errors.ts`**, called by the error handler. 1062, 1451 and 1452 map by constraint name to a specific code, with generic 409 codes as the fallback; 1406, 1264 and 1366 become 400. A constraint violation is never a 500 and never shows the driver's text. When adding a UNIQUE or FK, add its mapping there. Services still pre-check the common cases to give details.
- **Public API (T-08, details in `server/README.md`):**
  - Read-only `GET /public/...`, no session: deportes, competiciones (and their equipos and posiciones), partidos (fixture and detail), equipos/:id with squad. It is Módulo Informativo only (`services/public.service.ts`): it must never read or import users, sessions, coins, bets or audit.
  - Scores and goals are shown only once a match is `finalizado` with both sides loaded (otherwise both are null, and standings skip it). Cancelled matches are listed with their state. Standings are computed per request (3/1/0, only `finalizado`, ordered by points, goal difference, goals for, name, id; positions never shared).
  - Answers embed what a card needs (teams inside matches, team inside each standing row), so `src/lib` can map them without N+1 calls. The mapping to `src/types` for T-22 is in the README.
  - The API has its own per-IP limit (`PUBLIC_RATE_LIMIT_*`, 120/min by default) and is skipped by the global limiter. Successes carry `Cache-Control: public, max-age=30`; everything else in the API is `no-store`, errors and every 429 included (the no-store default is the first middleware in `app.ts`).
- **A malformed `%` in a route param** is 400 `INVALID_URL_ENCODING`, not logged. `error-handler.ts` recognizes only the router's own decode error; any other `URIError` stays a logged 500.
- **Matches (T-07, details in `server/README.md`; results are T-12, below):**
  - `/admin/partidos`: CRUD. Creating a match writes both `partido_equipo` rows (goals NULL) in the same transaction, always `programado`.
  - Dates are ISO 8601 with zone and seconds (`schemas/matches.schema.ts`), stored in UTC, and must be in the future.
  - **No manual state changes (T-13, user decision):** `POST /:id/estado` is gone (404). `en_curso` comes by itself at the kick-off, `finalizado` only from T-12's confirmation and `cancelado` only from T-16.
  - **Effective state everywhere** (`lib/match-state.ts`): `effectiveState` in TypeScript and `effectiveStateCondition` in SQL (filters). A stored `programado` whose `fecha_hora <= now` is `en_curso` in every answer, filter and rule; never compare `ep.codigo` to `'programado'` directly. `DURACION_PARTIDO_MINUTOS` (60) and `hasEnded` live there too.
  - `finalizado`/`cancelado` lock the match (409 `MATCH_LOCKED`). Competition, teams and date change only before the kick-off (a started match can't be postponed).
  - With bets, teams and competition never change, and the date can only be postponed (BR-014 precision).
  - Delete only before the kick-off (or if cancelled), with no bets, goals, loaded result or media.
  - The match row is locked (`FOR UPDATE`) for every write; betting code must lock it too.
  - The bets check is the injected `MatchBetsProbe` (Polla's `services/bets-match-probe.service.ts`, wired in `routes/index.ts`). T-07 never writes goals.
- **Match order (BR-013) is defined once, in `lib/match-order.ts`:** upcoming (`fecha_hora >= now`) soonest first, then past most recent first, then id. Every match list (T-07, T-08, T-19, T-21) uses `proximityOrderBy`. The 24 h betting close lives in `lib/betting.ts` (`bettingCloseTime`, and `isBeforeBettingClose`, the only comparison against it: the close instant itself is already closed).
- **Selections (T-09, Módulo Polla, details in `server/README.md`):**
  - `/apuestas` uses `requireAuth, requireBettor`, except `GET /apuestas/partidos` (`requireParticipant` since T-19: a pending user sees the list, an admin gets 403 `NOT_A_PARTICIPANT`). `GET /apuestas/partidos` lists every match with `apuesta: { estado, cierre, pronosticosAdmitidos }` (BR-052 states `disponible`/`cerrada`/`en_curso`/`finalizado`/`cancelado`), filterable by sport, competition, dates and betting state, in BR-013 order. `POST /apuestas/vista-previa` evaluates a proposed ticket and writes nothing.
  - `services/betting.service.ts` is the one place that checks selections: the match exists, is `programado` and before its close, and a draw (as a result or a tied exact score) only where `deporte.permite_empate`. Problems are reported per selection (`errores`, `valida`) plus `INSUFFICIENT_BALANCE` for the ticket. `valido` is what T-10 must require. A malformed shape is a 400.
  - Limits and rules live in `lib/betting.ts`: `MAX_GOLES_PRONOSTICO` (999), `MAX_SELECCIONES_POR_TICKET` (50), bet type and result codes. Identical repeated selections are allowed and flagged with `repiteA`. The cost is `COSTO_POR_SELECCION` per selection, valid or not.
  - T-10 must call `evaluateTicketInTransaction` first inside its `withTransaction`: it locks the user row `FOR UPDATE` (same lock as the debit), then the match, competition and sport rows `FOR SHARE`. On a non-transaction connection it rejects the promise. A `BETTING_CLOSED` error carries `cierre` in its own field.
- **Tickets (T-10, Módulo Polla, details in `server/README.md`):**
  - `POST /apuestas/tickets` (`requireBettor`, CSRF, required `Idempotency-Key` UUID header) confirms a ticket in one `withTransaction` (`READ COMMITTED`): lock the user, look up the key, `evaluateTicketInTransaction`, insert the ticket and its `pendiente` selections, `debitSelections`. Any invalid selection or a short balance is 409 `TICKET_REJECTED` with the preview-shaped evaluation in `details`, and nothing is written.
  - Idempotency (D20): the same key with the same selections returns the same ticket (200, `Idempotent-Replayed: true`); with other selections it is 409 `IDEMPOTENCY_KEY_REUSED`. The key lives as long as the ticket, is per user, and a rejected confirmation leaves it free. A deadlock retry reruns the key lookup too.
  - `GET /apuestas/tickets/:id` is the receipt (BR-025) for its owner only: anyone else, an admin included, gets the same 404 `TICKET_NOT_FOUND` as for a missing id. The ticket state (`ticketState` in `lib/betting.ts`: `pendiente` / `finalizado` / `anulado`), coins used and returned, and points are computed on read.
  - Modifying or cancelling before confirming is frontend only (BR-024): the preview writes nothing, and a confirmed ticket is never edited.
- **My bets (T-11, Módulo Polla, details in `server/README.md`):**
  - `GET /apuestas/mis-apuestas` (paginated, one row per selection with its ticket's totals, newest ticket first, not BR-013 order) and `GET /apuestas/mis-apuestas/resumen`. Guard: `requireParticipant` (a `pendiente` user gets an empty list, an admin 403 `NOT_A_PARTICIPANT`). Every query is anchored on the session user's `ticket.usuario_id`. No key, fingerprint, email or balance is ever returned.
  - Computed with the receipt's helpers (`selectionFrom`, `realResult`, `ticketTotals` in `services/tickets.service.ts`), never stored. `resultadoReal` only for a `finalizado` match with both sides loaded (BR-049 rule). The ticket state filter uses `ticketStateCondition`, the SQL twin of `ticketStateFromCounts` (a test keeps them equal).
  - "Aciertos" means selections in state `acertada` of any type, and points are `SUM(puntos_obtenidos)` (BR-042 precision); T-15's ranking must use the same definitions.
  - The list reads a page of ids from `ticket`/`seleccion` first, then only those rows, all inside one read-only snapshot (`withReadSnapshot`). The summary is a single statement, so its figures always agree with each other. Indexes `idx_ticket_usuario_fecha` and `idx_seleccion_ticket_estado` (covering) serve it.
- **Results (T-12, Módulo Informativo, details in `server/README.md`):**
  - Under `/admin/partidos/:id/resultado`: `GET` is the preview (BR-030, no effects), `PUT { golesLocal, golesVisitante }` loads or corrects the score, and `POST .../confirmar { confirmar: true, golesLocal, golesVisitante }` confirms it for good.
  - A score is loaded from the kick-off on (effective `en_curso`; the stored state is written as `en_curso`; the kick-off is checked by itself too, whatever the stored state says, and the same goes for goals and media), during and after the match, with no deadline (user decision), and never below the goals already attributed (409 `SCORE_BELOW_GOALS`). It stays private until confirmed.
  - Confirming is only allowed once the match's 60 minutes are over (409 `MATCH_NOT_ENDED`). It locks partido → competicion → deporte and requires both sides, the previewed score (else 409 `RESULT_CHANGED`) and no draw in a sport without draws. It sets `finalizado` and runs the injected `MatchSettler` (Polla's `settleMatchBets`, T-14, below) once, in the same transaction; any error rolls everything back.
  - After that, every route refuses the match with 409, goals included; media can still be added or removed (it changes neither result nor points). The preview also reports `avisos` (goals without a scorer, not blocking) and `confirmableDesde`.
  - The general result is computed only in `lib/match-result.ts` (`resultOfScore`, `officialResult`), never stored. The public match card now carries `resultado`, shown only with the official result. Audit actions: `registrar_resultado_partido` and `confirmar_resultado_partido`.
- **Goals and media (T-13, Módulo Informativo, details in `server/README.md`):**
  - Goals under `/admin/partidos/:id/goles`: scorer enrolled in that team and competition (409 `PLAYER_NOT_IN_TEAM` / `TEAM_NOT_IN_MATCH`), minute 1–120, from the kick-off until the confirmation, and never more per side than the loaded score (409 `GOALS_EXCEED_SCORE`).
  - **Images are uploaded, never linked:** multipart, one file in the field `imagen`, at most `UPLOAD_MAX_BYTES`. The type comes from magic bytes (JPEG/PNG/WebP/GIF only, never SVG). `lib/images.ts` re-encodes with sharp to WebP (1600 px max, `limitInputPixels` = `UPLOAD_MAX_PIXELS`, 24 MP by default, no metadata), one image at a time per process (`inImageQueue`, so two big uploads never decode together), and `services/media-storage.ts` stores it under a random name in `UPLOADS_DIR` (a Docker volume). The `server` container runs as the unprivileged `node` user, which owns `/data/uploads`.
  - **Files never go inside a transaction or an audit hook:** save before, remove the new file if the transaction fails, remove replaced or deleted files after the commit. A deadlock retry must not write files.
  - Images are served read-only by `GET /admin/archivos/:nombre` (any match) and `GET /public/archivos/:nombre` (only official results). Only names a row points at, with a fixed `image/webp`, `nosniff`, sandbox CSP and `Cross-Origin-Resource-Policy: cross-origin`. The public ones are cached for one hour (`PUBLIC_IMAGE_MAX_AGE_SECONDS`): a removed image may still show that long in a cache that had it.
  - **Videos are links only** (`lib/video-links.ts`, the one list of allowed hosts: YouTube and Vimeo), normalized, never fetched by the server. The frontend may only embed `video.embedUrl`.
  - Media (per goal: one image and one video; per match: up to 20 images and 10 videos) is allowed from the kick-off, also after the result is confirmed, never in a cancelled match. Public answers show goals and media only with the official result (BR-049).
- **Settlement (T-14, Módulo Polla, details in `server/README.md`, "Liquidación de apuestas"):**
  - `settleMatchBets` (`services/bets-settlement.service.ts`) runs inside the confirmation. Every `pendiente` selection of that match becomes `acertada` or `no_acertada` with its points, each on its own (BR-038). Voided or already settled selections, and the ticket's selections on other matches, never change. It never moves coins (BR-039).
  - Points live only in `lib/points.ts` (`PUNTOS_GANADOR` 3, `PUNTOS_EMPATE` 1, `PUNTOS_MARCADOR_EXACTO` 3, `PUNTOS_FALLO` 0). `settleSelection` is the rule in TypeScript; the service applies the same rule as one SQL `CASE`, and a test keeps both equal.
  - Few statements: the pending ids through `idx_seleccion_partido_estado` (pinned with the optimizer hint `pendingIndexHint`, never `FORCE INDEX`: with stale statistics MySQL picked `fk_seleccion_estado`, and a missing index must only cost speed, not a 500), read without a lock (the match row is already `FOR UPDATE`), then one UPDATE by primary key per 1000 ids that checks `pendiente` again. A second run in the same transaction settles nothing; a deadlock retry settles once.
- **Ranking (T-15, Módulo Polla, details in `server/README.md`, "Ranking de la polla"):**
  - `GET /ranking` for any session (a `pendiente` user or an admin gets `propia: null`): `{ top, topSinMostrar, propia, participantes, posicionesTop, maxFilasTop }` (`propia.enLista` says whether the own row is in `top`), rows `{ posicion, participante: { nombre }, puntos, aciertos, esPropia }`. Only the display name: never email, balance, state or another user's id. There is no public version (it ties names to betting results).
  - Admin: `GET /admin/polla/ranking` (the whole ranking, paginated, with ids) and `GET /admin/polla/estadisticas` (participants, tickets and selections by state, coins, points; every `apostador`, never admins).
  - T-21: `GET /admin/polla/apuestas` (`services/admin-bets.service.ts`), the bets placed, read only: one row per selection with the history's shape (T-11) plus `usuario: { id, nombre }`, the history's filters plus `usuarioId`, newest ticket first, only `apostador` tickets, never an email, balance, key or fingerprint. It reuses `HISTORY_COLUMNS`, `ticketAggregatesFor` and `myBetFrom` from `bet-history.service.ts`. Each admin ranking row also carries `empatados`, how many share that position in the whole ranking: a page of 20 can't tell on its own.
  - Computed on every read (`services/ranking.service.ts`), one statement with `RANK()` over covering indexes (about 25 ms with 300 participants and 30 000 selections). `rankParticipants` in `lib/ranking.ts` is the same rule in TypeScript, kept equal by a test. Points and hits use T-11's definitions.
- **Cancellation (T-16, Módulo Polla, details in `server/README.md`, "Cancelación de partidos"):**
  - `GET /admin/partidos/:id/cancelacion` (preview, no effects) and `POST /admin/partidos/:id/cancelacion/confirmar { confirmar: true }`. `services/match-cancellation.service.ts` (Polla) calls Informativo's `markCancelled`; Informativo never imports it.
  - A `programado` or `en_curso` match can be cancelled, with goals or media too; `finalizado` is 409 `MATCH_ALREADY_FINISHED`, `cancelado` 409 `MATCH_ALREADY_CANCELLED`. The preview and the answer report `seleccionesSinDevolucion: { total, sinDebito, cuentaAdministrador }`. Cancelling is final: `cancelado` locks the match like `finalizado`, and its score, goals and media are never public.
  - One `READ COMMITTED` transaction, lock order usuario → partido: read the affected `apostador` users without a lock (admin accounts are never locked: they get no refund), lock them by id, lock the match, read the pending selections again and **start over** (up to 3 times, then 409 `CONCURRENT_UPDATE`) if an `apostador` that wasn't locked appears. `ticketsAnulados` is one aggregate per ticket, not a check per selection. Then void by primary key in batches, `refundSelectionsBatch`, `markCancelled` and the audit hook (`cancelar_partido`).
- **Audit (T-17, Módulo Auditoría, details in `server/README.md`, "Auditoría"):**
  - `services/audit.service.ts` (`auditHooks`, `participantAuditHooks`, injected by `routes/index.ts`) inserts one `auditoria` row per admin write, in the action's own transaction: a refused or failed action leaves none, a failed insert rolls the action back (500), a deadlock retry leaves one. It only writes to the database.
  - `lib/audit.ts` is the one map from app action to catalog code and entity (a test keeps it equal to `02-catalogos.sql`), and builds the detail: changed fields with before and after, the created or deleted row, the score, the cancellation figures. Rows carry only stored fields (`soloGuardados` in `services/audit.service.ts`: no `cierreApuestas`, joined names, `lado`, `embedUrl` or derived result). An edit that changes nothing, or the same score registered again, records nothing (D-004); `cambios` decides that on the real values and only cuts what it shows, marking a cut side `recortado`. `claveProhibida` drops any key with a whole forbidden word (password, hash, token, secret, clave, huella, idempotency, cookie, session, email/mail/correo, saldo, csrf, apikey, authorization, credential, pin…), splitting camelCase, separators, digits and glued known words, never by substring (`hashtag`, `passport`, `mailing` stay); a flag such as `emailVerificado` is kept only with a boolean value. Strings are cut by code point (never splitting an emoji: a lone surrogate makes MySQL refuse the JSON and roll the action back) and lone halves become U+FFFD. Depth 8, 20 array items, 50 keys; the detail must fit 3000 bytes of text and 3800 bytes by `tamanoBinario` (an upper bound of MySQL's binary size, the CHECK allows 4096), else it keeps only field names marked `recortado`.
  - `recordAudit` checks the author is an admin, the code exists with the expected entity and the affected row exists (except on deletes). Records are never updated or deleted, not even when their row is deleted.
  - `GET /admin/auditoria` (admin only): newest first, paginated, strict filters `accion`, `entidad`, `entidadId` (needs `entidad`), `usuarioId`, `desde`, `hasta`. The admin shows as id and name, never the email. There is no write route. It reads the page of ids from `auditoria` alone (with no filter or only dates, pinned to `idx_auditoria_fecha` read backwards, no sort), then those rows with their joins.
  - `admin:create` (`services/admin-bootstrap.service.ts`) creates or promotes in a transaction and records it there, with the account itself as author and entity (D-005); nothing when it already was an admin. A run that loses a race answers `unchanged` (exit 0) or, if a registration took the email, a clear error; never the driver's text, and never a silent promotion of that account.
  - Locks: two plain reads and one INSERT, whose FKs take S on the admin's `usuario` row and the catalog row; nothing takes X on those, so it can't close a cycle. It never touches tickets.
- **Locking and concurrency (T-09 fix, details in `server/README.md`, "Orden de bloqueo y concurrencia"):**
  - Lock with one statement per table, by primary key only (`SELECT id FROM t FORCE INDEX (PRIMARY) WHERE id IN (?) ORDER BY id FOR ...`). Never lock through a join or a secondary index: the plan can change and lock index ranges (the ticket once deadlocked with a state change that way). Lock only ids that exist: under REPEATABLE READ a missing id takes a gap lock that blocks inserts (`lockAndReadMatches` filters them first).
  - Fixed order across tables: usuario → partido → plantel → equipo → jugador → competicion → deporte, ascending id within a table. Read the facts a rule depends on with a locking read, after locking.
  - `withTransaction` reruns the whole transaction on a deadlock (1213), up to 3 attempts, so transaction work must only touch the database. A persistent deadlock or a lock wait timeout (1205) is 409 `CONCURRENT_UPDATE`, never a 500.
  - `tests/concurrency-stress.test.ts` must see zero deadlocks. `tests/betting.test.ts` checks, through `performance_schema.data_locks`, that the ticket only takes record locks on primary keys.
- **Participants (T-04, details in `server/README.md`):**
  - Admin actions live in `services/participant-validation.service.ts`. Each runs through `withTransaction` (`db/transaction.ts`) with `READ COMMITTED` and changes state only through an `UPDATE ... WHERE <expected state>`, so a repeated or concurrent call gets 409 without side effects.
  - Their `hooks.inTransaction(conn, outcome)` writes the audit record (T-17, `participantAuditHooks`): same transaction, before commit.
  - Coin amounts go in `lib/coins.ts`, never inline.
  - Participant points are always `SUM(seleccion.puntos_obtenidos)` in the query.
- **Docker dev reload:** the `server` compose service bind-mounts the source, but a Windows-host bind mount doesn't deliver the native filesystem events `tsx watch` needs. It runs `dev:docker` (`nodemon --legacy-watch`, polling-based) instead; bare local dev (`npm run dev` in `server/`, no mount involved) keeps the faster `tsx watch`. After adding a new dependency, if the container comes up with `<package>: not found`, the anonymous `node_modules` volume is stale — `docker compose up -d --force-recreate -V server` forces it to pick up the new image's install.

## Development

```
npm run dev             # Vite dev server, http://localhost:5173
npm run build           # tsc -b && vite build → dist/
npm run preview         # serve dist/ locally
npm test                # frontend tests (Vitest + Testing Library, jsdom)
npm run server:dev      # backend, http://localhost:3001 (needs `docker compose up -d db`)
npm run server:test     # backend test suite (Vitest + Supertest)
npm run server:typecheck # backend type check, src + tests
npm run server:admin:create # create/promote an admin (ADMIN_EMAIL, ADMIN_NOMBRE; asks for the password)
npm run server:coins:check   # balance vs SUM(movements) per participant; exit 1 on mismatches
npm run server:seed:dev -- --yes-dev-data       # development only (NODE_ENV=development, DEV_SEED_DATABASE): load the sample data (D-013, D-016)
npm run server:seed:dev:clean -- --yes-dev-data # development only: remove only the marked sample data
```

Start the dev server as a background process so it doesn't block the session, and stop it when done. `npm run build` must finish with no TypeScript errors or warnings. For the backend, `docker compose up -d` (from the repo root) runs both `db` and `server` together, with reload — see `server/README.md` for the full command reference and layer conventions.

## Documentation

- [Vite](https://vite.dev/guide/): config, static assets, CSS Modules, plugins, building for production.
- [React](https://react.dev/reference/react): components, hooks, `<dialog>` and form handling.
- [React Router, data mode](https://reactrouter.com/start/data/routing): routes, loaders, error boundaries, navigation.
- [sharp](https://sharp.pixelplumbing.com/api-resize): the resize options the pixel-images plugin mirrors.
- [Express 5](https://expressjs.com/en/5x/api.html): routing, error-handling middleware, async handler forwarding.
- [zod](https://zod.dev/): schema validation, used for both env config and request bodies (`server/src/schemas/`).
- [OWASP cheat sheets](https://cheatsheetseries.owasp.org/): Password Storage, Session Management and CSRF Prevention, which the T-03 auth design follows.
- [mysql2](https://sidorares.github.io/node-mysql2/docs): promise pool, prepared statements.


## Betting platform
Construir una plataforma web de polla deportiva con autenticación, roles, monedas virtuales, apuestas, resultados, ranking y administración de partidos.

El agente debe leer antes de desarrollar:

@business-rules.md

### Reglas generales de trabajo para Betting platform

1. Implementar una tarea a la vez.
2. No avanzar a la siguiente tarea si la actual tiene errores.
3. Toda regla de negocio crítica debe validarse en backend.
4. No confiar únicamente en validaciones frontend.
5. Toda funcionalidad nueva debe incluir pruebas.
6. No modificar reglas de negocio sin actualizar `business-rules.md`.