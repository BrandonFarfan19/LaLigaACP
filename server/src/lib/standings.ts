/**
 * Informative standings points (per competition). Unrelated to the pool's
 * points (BR-039). Also in CLAUDE.md "Business rules (backend)".
 */
export const PUNTOS_VICTORIA = 3;
export const PUNTOS_EMPATE = 1;
export const PUNTOS_DERROTA = 0;
