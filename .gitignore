# build output
dist/

# dependencies
node_modules/

# uploaded images in local development (T-13; in Docker they live in a volume)
server/.data/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

.wrangler/

# Volcado de datos reales para producción (D-04), generado con
# `npm run db:export-real`. No se versiona: son nombres de 102 personas reales,
# y además es un artefacto derivado de la base de desarrollo, no una fuente.
# Se genera y se copia al servidor con scp; el README explica los tres pasos.
db/datos-reales.sql

# Fotos originales de jugadores (pesadas); en public/jugadores van las versiones reducidas
fotos-originales/
